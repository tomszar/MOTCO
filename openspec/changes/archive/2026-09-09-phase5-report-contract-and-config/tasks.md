## 1. Report contract in the configuration

- [x] 1.1 Add a frozen `ReportContract` dataclass (`driver_component` ∈ {observed, pls_captured, residual}, `cross_replicate_driver_agreement` fixed to `descriptive`, `n_jobs_override` ∈ {forbid, warn} default `warn`) and an optional `report_contract` field on `StudyConfig` in `src/motco/simulations/study/config.py`, with `_build_report_contract` rejecting unknown fields/values and `_config_to_dict` emitting the block; verify in `tests/test_study_config.py` with a valid block, an unknown field, an illegal `n_jobs_override`, a `claim` agreement value, and a dump → reload round trip.
- [x] 1.2 Make `_build_config` reject unknown root keys with an error naming them; verify with a test that a stray root key fails to load and that every JSON under `examples/trajectory_power_study/` still loads (extend the load-all / cell-identity snapshot test in `tests/test_study_design_grid.py`).

## 2. Driver reporting and contract echo

- [x] 2.1 Add `build_driver_report(attribution_frame, contract)` to `src/motco/simulations/study/phase4.py` (declared component only; columns per design D2; no `top_k_jaccard`/`sign_agreement`) and `write_report_contract(config, records, out_dir)` to `src/motco/simulations/study/report.py` (driver component, descriptive-agreement statement, uniform `n_jobs` from records with a `ValueError` naming non-uniform values, anchor `cell_id`/`resolves_modes`/`counted_as: 1`); verify in `tests/test_study_phase4.py` / `tests/test_study_report.py` with a three-component fixture (observed-only rows, no cross-replicate columns), a non-uniform `n_jobs` fixture (raises), and an anchor fixture (echo names the cell and modes).
- [x] 2.2 Make `render_attribution_stability` accept the contract: under a contract plot only `bootstrap_sign_stability_mean` / `bootstrap_top_k_frequency_mean` for the declared component with the title "Within-replicate bootstrap stability (<component> component)"; without a contract render exactly as today; verify with a test that the contract path writes the figure and the no-contract path is unchanged.
- [x] 2.3 Wire `scripts/motco_study.py report` to write `report/driver_report.csv` and `report/report_contract.json` only when `config.report_contract` is set and print their paths; verify the committed Phase 4, design-point, and ladder fixtures re-report byte-identically with no new files, and that a contract fixture writes both.

## 3. Worker-count lock

- [x] 3.1 In `scripts/run_study_shard.py`, when `config.report_contract.n_jobs_override == "forbid"` and `--n-jobs` differs from `config.evaluation.n_jobs`, print both values and the signature consequence to stderr and return exit code 2 before enumeration; keep the warning path for `warn`/no contract and accept an equal value; verify in `tests/test_study_end_to_end.py` (or a new runner test) with a forbidding config: differing value → exit 2 and no shard file written, equal/absent → shard runs.
- [x] 3.2 Update the header comment of `scripts/motco_study_array.sbatch` to state that a config with `n_jobs_override: forbid` refuses `STUDY_N_JOBS`; verify by reading the rendered comment (no logic change).

## 4. Phase 5 paper-grade profile and report template

- [x] 4.1 Commit `examples/trajectory_power_study/phase5_power_study.json` per design D4 (generator and `evaluation.integration_params` copied from the ladder profile; no `design_grid`; no `surgery_censoring` key; effects 0/0.25/0.5/0.75/1.0; 500 replicates; 999 permutations; `n_jobs` 1; new `base_seed`; matched seeds family `phase5-primary` with shared anchor; attribution on nonzero orientation primary cells, 100 bootstraps, top-20, seed 0; gate enabled with the Phase 4 roles; acceptance `type_i` 0.05/2.0, `power` 0.80 on the three diagonals, `specificity` = translation × 3 + magnitude/`angle` + magnitude/`shape`; `report_contract` observed/descriptive/forbid; metadata with `derives_from`, intent, and notes covering the dropped specificity targets, the five-point axis and headroom, attribution cost, the anchor, and the no-`--n-jobs` rule); verify with `tests/test_phase5_power_study_profile.py`: loads, 19 cells (2 Type I controls + anchor + 16 power), 9,500 units, no headroom rejection, no `clamp`, no orientation/`shape` specificity target, gate roles as declared, contract values, generator/integration params equal to the ladder's, and add the profile to `tests/data/study_cell_identity_snapshot.json`.
- [x] 4.2 Commit `examples/trajectory_power_study/phase5_report_template.md` per design D5 (all required sections, `PROVENANCE.txt` field list, reproduction block without `--n-jobs`, `phase4_*` output-name note); verify with a test that the template contains each required section heading and names the profile's `driver_component` and the descriptive-agreement statement.
- [x] 4.3 Run a one-replicate rehearsal of the profile locally (derived, uncommitted copy with `n_replicates` 1 and `permutations` 999 under the scratch/results dir; 19 units; no `--n-jobs`) through shard → merge → report; verify `driver_report.csv`, `report_contract.json`, the gate decision file, and attribution diagnostics on the orientation units exist, and record the per-unit wall time (CV + 999 permutations + attribution at n = 1200) for task 5.1.

## 5. Documentation

- [x] 5.1 Update `src/motco/simulations/study/README.md` (configuration table row for `report_contract`, unknown-key strictness, outputs tree with `driver_report.csv` and `report_contract.json`, runner refusal, Phase 5 cost line from task 4.3) and `examples/trajectory_power_study/README.md` (config quick reference; a "Phase 5 paper-grade study" section with the SLURM/local recipe, the template, and the cost estimate; `study.json` marked superseded and why); verify by reading the rendered sections.
- [x] 5.2 Update `docs/phase5-readiness.md` (item 5 resolved with the profile, template, and the contract field enforcing each of its four points; header status line), `docs/roadmap.md` ("Not yet established": remove the item-5 and unwritten-config bullets; "Next three changes": run becomes #1, Phase 6 #2, and add the successor), and the `study/` bullet in `CLAUDE.md`; verify the readiness header lists item 5 as resolved and the roadmap's next change is the paper-grade run.
- [x] 5.3 Run the pre-commit gate (`uv run ruff check src/ tests/ && uv run mypy src/motco/ && MOTCO_TEST_PERMS=99 uv run pytest tests/ -m "not slow" --tb=short`) and verify all pass.
