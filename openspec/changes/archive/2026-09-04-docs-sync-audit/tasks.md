## 1. D8 — Numeric-aware level ordering (the one code change)

- [x] 1.1 Add a private `_sort_levels(labels)` helper in `src/motco/stats/design.py`: numeric
      order when every distinct label parses as an integer, lexicographic otherwise; fall back to
      lexicographic for the whole factor if two distinct labels collide numerically (e.g. "01" vs
      "1"). Use it for `g_levels`/`l_levels` in `get_model_matrix` and update the docstring's
      category-order sentence. Verify: `uv run pytest tests/test_design.py -v` passes unchanged.
- [x] 1.2 Use the same helper for `group_levels`/`stage_levels` in
      `build_simulation_trajectory_design` (`src/motco/simulations/evaluation.py:385-386`) so
      harness and core cannot disagree. Verify: `uv run pytest tests/test_evaluation.py -v` (or
      the harness test file) passes unchanged.
- [x] 1.3 Add a regression test: metadata with stage labels "0".."11" produces stage-level order
      0,1,2,...,11 in the model matrix columns, LS-mean rows, and contrast indices (not the
      lexicographic "0","1","10","11","2",...); plus a non-numeric-label case pinning
      lexicographic fallback and a leading-zero collision case pinning the fallback. Verify: new
      test passes and fails against the pre-change `sorted()`.
- [x] 1.4 Confirm byte-identity for existing configs: run the fast suite
      (`MOTCO_TEST_PERMS=99 uv run pytest tests/ -m "not slow" --tb=short`) — all regression
      reference values must pass untouched.

## 2. D1/D7 — CLAUDE.md and evaluation docstring

- [x] 2.1 D1: fix `CLAUDE.md` trajectory line — `_estimate_shape` uses strict pairwise ordinary
      Procrustes alignment over all orthogonal transforms (reflections aligned away), not
      "iterative Procrustes GPA". Verify: `grep -n "GPA" CLAUDE.md` returns nothing.
- [x] 2.2 D7: rewrite the latent-space passage in `CLAUDE.md` and the module docstring of
      `src/motco/simulations/evaluation.py` per design.md — PLS is the production measurement
      space; SNF remains a supported integration path, deferred for the Euclidean trajectory
      statistics pending graph-native statistics (align with `docs/roadmap.md`). Verify:
      `grep -rn "production latent-space method" CLAUDE.md src/` returns nothing and the three
      documents state the same status.

## 3. D2/D3 — README examples

- [x] 3.1 D2: fix the `motco simulate` CLI example in `README.md` — remove
      `--prop-affected-features`, replace the "requires R + InterSIM" comment with the numpy
      generator wording, use only flags the parser accepts. Verify: run the example command with
      `--out-dir` pointed at a temp dir; it exits 0.
- [x] 3.2 D3: rewrite the README Python example using `SemiSyntheticTrajectoryParams`,
      `generate_semisynthetic_trajectory`, and `evaluate_semisynthetic_trajectory` with
      currently existing parameters. Verify: paste the snippet into `uv run python` in a fresh
      shell; it executes without error.

## 4. D4/D5/D6 — Roadmap, spec purpose, matched-seed wording

- [x] 4.1 D4: move the resolved shape-invariance bullet in `docs/roadmap.md` from "Not yet
      established" to "Established" (keep the wording that names `unify-shape-reflection-policy`).
      Verify: the bullet appears exactly once, under "Established".
- [x] 4.2 D5: edit the purpose line of
      `openspec/specs/semisynthetic-trajectory-generator/spec.md` — datasets are generated by the
      numpy generator from the cached InterSIM reference (no R at runtime), not "from InterSIM
      outputs". Verify: `openspec validate` still passes.
- [x] 4.3 D6: narrow matched-seed claims with the design.md wording in (a) the
      `derive_replicate_seed` docstring in `src/motco/simulations/grid.py`, (b) the
      `generate_showcase_datasets` docstring in `src/motco/simulations/showcase.py`, and (c) the
      "same generator draws" note in
      `examples/trajectory_power_study/angle_pivotality_diagnostic.json`. Verify:
      `grep -rn "same dataset\|same generator draws" src/ examples/` shows only the corrected
      phrasing.

## 5. D10 — Toy-dataset spec (found during implementation)

- [x] 5.1 Correct `openspec/specs/toy-dataset/spec.md`: the fixture is pinned and predates the
      numpy generator, so drop the false "canonical regeneration command" (it passed the removed
      `--prop-affected-features` flag) in favor of a current-CLI equivalent marked as a distinct
      dataset, and reword the `truth.json` requirement to stop citing a nonexistent flag. Add a
      scenario requiring every published `motco simulate` command to be accepted by the shipped
      parser. Verify: `openspec validate --specs` passes and
      `grep -rn "prop-affected-features" openspec/ README.md` returns nothing.
- [x] 5.2 Add `toy-dataset` to the proposal's Modified Capabilities with a delta spec carrying the
      full updated requirements. Verify: `openspec validate docs-sync-audit --strict` passes.

## 6. Gate

- [x] 6.1 Run the full pre-commit gate: `uv run ruff check src/ tests/ && uv run mypy src/motco/
      && MOTCO_TEST_PERMS=99 uv run pytest tests/ -m "not slow" --tb=short`. Verify: all three
      pass.
- [x] 6.2 `openspec validate --strict` passes for the change. Verify: command exits 0.
