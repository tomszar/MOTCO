## 1. Nested evaluation axes (grid + config)

- [x] 1.1 Extend `_split_axis`, `_apply_axis_value`, and `_get_axis_value` in `src/motco/simulations/grid.py` to accept the single nested form `evaluation.integration_params.<key>` (set the key in a copied mapping; `None` removes it; reading returns `mapping.get(key)`), rejecting any other multi-segment axis with an error naming it; verify with new `tests/test_simulation_grid.py`-style cases: value applied beside existing knobs, `None` removes the key, baseline read on absent key is `None`, `generator.a.b` and `evaluation.attribution.x` rejected.
- [x] 1.2 Verify two otherwise identical cells built with axis values `None` and `6` have different `parameter_signature`s, and that a cell built with `None` has a signature identical to one built without the axis at all (D2); add both assertions to the same test module.
- [x] 1.3 Extend `_validate_axis_namespace` and `axis_baseline_value` in `src/motco/simulations/study/config.py` to accept the nested form and resolve the baseline from `evaluation.integration_params` (absent → `None`), keep rejecting unknown namespaces and other nesting; verify in `tests/test_study_config.py` that a design grid `[null, 4, 6]` on the rank axis loads with baseline `None`, that `[4, 6]` is rejected naming the axis and the missing `null`, and that `null` survives `dump_study_config` → reload.
- [x] 1.4 Confirm every committed config in `examples/trajectory_power_study/` still loads and enumerates identically (cell ids and parameter signatures unchanged) by extending the existing enumeration-stability snapshot test; verify `uv run pytest tests/test_study_config.py tests/test_study_design_grid.py tests/test_study_phase4.py`.

## 2. Enumeration invariants for evaluation-only columns

- [x] 2.1 Add `tests/test_study_design_grid.py` cases on a small rank-only design grid asserting every design column's cells share `_generator_identity` and `derive_replicate_seed` with the baseline column's corresponding cells, that enumeration succeeds (no duplicate-dataset error, no headroom rejection), and that two cells with equal generator *and* evaluation identity in one family are still rejected; verify the tests pass with no change to `study/enumerate.py`.
- [x] 2.2 Run the end-to-end smoke test (`tests/test_study_end_to_end.py`) on a tiny rank-ladder config (1 replicate, 9 permutations, ranks `[null, 3]`) and verify shards run both CV and forced units, merge deduplicates, resume skips completed units, and forced records carry `component_selection = "forced"` with `selected_lv` equal to the forced rank.

## 3. Reporting: operating table column, ladder figure, rank decision

- [x] 3.1 Add a `component_selection` column (`cv`/`forced`/`mixed`) to `resolve_operating_by_design_point` in `src/motco/simulations/study/spectrum.py`, read from each record's integration metadata; verify in `tests/test_study_spectrum.py` that forced rows report `forced` with `median_selected_lv` equal to the rank, CV rows report `cv`, and the existing design-point fixture's frame is unchanged apart from the new column.
- [x] 3.2 Add `RankDecisionRule` (axis, target pair, protected pairs, `type_i_bound`, `gain_se_multiplier`, `loss_se_multiplier`) under `AcceptanceTargets.rank_decision` in `study/config.py`, validated so the axis is a declared design-grid axis whose values include `null`, the target/protected modes are in `trajectory_modes`, and multipliers are non-negative; verify with config tests for a valid rule, a rule without the axis in the design grid, an unknown protected mode, and round-trip through `dump_study_config`.
- [x] 3.3 Implement `evaluate_rank_decision(records, rule, alpha)` and `write_rank_decision` in `src/motco/simulations/study/targets.py` per design D5 (reference = `null` column; criteria a/b/c with pooled SE; verdict `keep_cv` or `adopt_fixed_rank` with the smallest qualifying rank; per-column per-criterion statuses with the rates, SEs, and thresholds used); verify with `tests/test_study_targets.py` cases: no rank qualifies, ranks 6 and 9 qualify → 6, protected veto names the pair, anchor veto names the statistic, thresholds read from the rule.
- [x] 3.4 Add `render_rank_ladder` to `src/motco/simulations/study/report.py` (per mode incl. the anchor, each statistic's rate vs rank with MC error bars, CV column at its median selected rank with a distinct marker/label), built from the design-point operating frame, written only when the rank axis is declared; verify in `tests/test_study_report.py` that a rank-axis fixture writes the PNG and a (ρ, n) fixture does not.
- [x] 3.5 Wire `report.py` to write `rank_decision.json`/`rank_decision.csv` only when `acceptance.rank_decision` is declared, confirm `render_design_point_power` degrades gracefully (or is skipped) on a rank-only grid, and confirm gate/acceptance/curves still read the baseline column only; verify existing Phase 4 and design-point fixtures re-report byte-identically and the rank fixture writes the new files.

## 4. Ladder profile and documentation

- [x] 4.1 Commit `examples/trajectory_power_study/phase5_latent_rank_ladder.json` per design D6 (derives from the design-point pilot; `n_samples` 1200, ρ 0.0, `p_dmp` 0.1, no `surgery_censoring` key; all four modes; effects 0/0.25/0.5/1.0; rank axis `[null, 3, 4, 6, 9, 12]`; 100 reps; 199 perms; `n_jobs` 1; matched seeds with shared anchor and a new family name; attribution off; gate off; `type_i` target kept; `rank_decision` block; metadata notes covering matched datasets, forced-3 rationale, cost, and the no-`--n-jobs` rule); verify with a profile test that loads the file, enumerates 78 power cells plus the baseline Type I controls with no headroom rejection, and asserts every design column shares generator identity and seeds with the baseline.
- [x] 4.2 Document the nested-axis syntax and `null` semantics, the shared-dataset pairing of evaluation-only columns, `rank_decision`, and the new outputs (`rank_decision.json`/`.csv`, ladder figure, `component_selection` column) in `src/motco/simulations/study/README.md` (configuration table, outputs tree) and `examples/trajectory_power_study/README.md` (config quick reference, a "Phase 5 latent-rank ladder" section with the SLURM/local recipe); update the `study/` bullet in `CLAUDE.md`; verify by reading the rendered sections.
- [x] 4.3 Run the pre-commit gate (`uv run ruff check src/ tests/ && uv run mypy src/motco/ && MOTCO_TEST_PERMS=99 uv run pytest tests/ -m "not slow" --tb=short`) and verify all pass.

## 5. Run the ladder and record the decision

- [x] 5.1 Run the ladder on SLURM (or locally in shards) with `scripts/run_study_shard.py` over `phase5_latent_rank_ladder.json`, `--error-policy record`, without `--n-jobs`, BLAS pinned to one thread, into `results/phase5-latent-rank-<date>/`; verify with `motco_study.py merge` that every enumerated (cell, replicate) unit is present once with a matching parameter signature and zero failures (or failures recorded per column and rerun), and write `PROVENANCE.txt` (config sha256, code revision, environment, unit timings).
- [x] 5.2 Generate the report (`motco_study.py report`) and verify `design_point_operating.csv` (with `component_selection`), `rank_decision.json`/`.csv`, the ladder figure, `realized_surgery.csv` (no censoring, no duplicated constructions across modes), and the baseline column's Type I table exist; check the CV column's orientation `angle` power at e = 1.00 agrees with the design-point pilot's ρ = 0, n = 1200 column within Monte Carlo error.
- [x] 5.3 Write `docs/reports/latent-rank-ladder-<date>.md` (config, code revision, versions, unit/failure counts, decision verdict with per-criterion table, per-mode rate-vs-rank for all three statistics beside eigengap and `angle` null-width dispersion, orientation→shape decay at the design point, re-measured magnitude/shape at CV rank with `p_dmp = 0.1`, anchor Type I per rank, limitations, reproduction commands); verify every number cites a CSV/JSON path under `results/phase5-latent-rank-<date>/report/`.
- [x] 5.4 Update `docs/phase5-readiness.md` item 3 (resolved; the committed group-blind rank rule; header status line), `docs/roadmap.md` (Phase 5 planned baseline names the rank rule; refresh "Not yet established" and "Next three changes"), and `CLAUDE.md` if the committed rule differs from CV; verify the readiness header lists item 3 as resolved with the report link and the roadmap's Phase 5 baseline names the rule.
