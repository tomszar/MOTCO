## 1. Configuration: `design_grid` and `acceptance.design_point`

- [x] 1.1 Add a `DesignGrid` dataclass (`axes: Mapping[str, tuple[Any, ...]]`) to `study/config.py`, parsed from an optional `design_grid` block, validated for namespace (`generator.`/`evaluation.`), non-empty values, no overlap with `axes`, and presence of the baseline value for every declared axis; verify with new `tests/test_study_config.py` cases for each rejection and for a valid grid round-tripping through `dump_study_config`.
- [x] 1.2 Add a `DesignPointDecisionRule` dataclass (`trajectory_mode`, `statistic`, `min_power_at_top`, `confirmation_se_threshold`, `prefer: tuple[str, ...]`) under `AcceptanceTargets.design_point` (optional), validated so `prefer` names only design-grid axes and the target mode is in `trajectory_modes`; verify with config tests for a valid rule, an unknown preference axis, and a rule declared without a design grid.
- [x] 1.3 Confirm every committed config in `examples/trajectory_power_study/` still loads and enumerates identically (cell ids and parameter signatures unchanged) by extending the existing enumeration-stability test to snapshot ids before/after; verify `uv run pytest tests/test_study_config.py tests/test_study_phase4.py`.

## 2. Enumeration: crossed design points

- [x] 2.1 In `study/enumerate.py`, add `_design_point_cells(config, resolver)` that crosses `design_grid.axes`, skips the baseline point, and for each other point emits a `power_design` zero-effect anchor plus (mode × nonzero effect) `power_design` cells with `design_point` coordinates and the existing `trajectory_mode`/`effect_size`/`zero_effect_anchor`/`resolves_modes` metadata; verify with a test that a 2×2 grid yields 3 × (1 + modes × nonzero effects) design cells and no cell for the baseline point.
- [x] 2.2 Extend `_assign_seed_families` so `power_design` cells join `matched_seeds.primary_family`; verify with a test that design cells and primary cells share `derive_replicate_seed` at the same replicate index and that `_require_distinct_primary_datasets` still rejects two identical-identity cells while accepting the per-column anchors.
- [x] 2.3 Verify `_require_surgery_headroom` rejects a design grid whose ρ = 0 column requests an over-headroom orientation effect at `p_dmp = 0.2` while the same effect enumerates at a ρ = 0.9 column, with a test asserting the error message names the offending cell and saturating effect.
- [x] 2.4 Add `power_design` to `spectrum.POWER_PHASES` and to any phase allow-lists in `sharding.py`/`merge.py`/`summary.py`, then run the end-to-end smoke test (`tests/test_study_end_to_end.py`) on a tiny design-grid config (1 replicate, 9 permutations) to verify shards run, merge deduplicates, and resume skips completed design units.

## 3. Reporting: design-point tables and decision

- [x] 3.1 In `spectrum.py`, add `resolve_operating_by_design_point(records, alpha)` producing one row per (design coordinates, mode, effect, statistic) with rejection rate, MC SE, eigengap mean/terciles, `angle` null `q95` median/IQR/SD, and selected-component median/min/max, reading the baseline column from `power_primary` records under the baseline coordinates and anchors as `none`/`0.0`; verify with `tests/test_study_spectrum.py` cases on synthetic records, including an empty frame when no `power_design` record exists.
- [x] 3.2 Rekey `resolve_orientation_by_continuity` on (ρ, other design coordinates, mode, effect) and emit the extra coordinate columns only when present; verify with a test that a design-grid record set yields separate rows per (ρ, n) and that a continuity-only record set produces a frame identical to the current output (snapshot the existing test fixture).
- [x] 3.3 In `targets.py`, add `evaluate_design_point_decision(records, rule, alpha)` returning per-column classification (`meets`/`marginal`/`fails`), anchor rejection rates per statistic, and the verdict (chosen column in preference order or `revise_claim`); verify with `tests/test_study_targets.py` cases covering a confirmed pick, a marginal-only grid, a preference-order tie-break, and thresholds read from the rule rather than defaults.
- [x] 3.4 Wire `report.py` to write `design_point_operating.csv`, `design_point_decision.json`, and `design_point_decision.csv` only when design records exist, and confirm `build_report_frames`/gate/acceptance still read the baseline column only; verify with `tests/test_study_report.py` that a Phase 4 fixture re-reports byte-identically and a design-grid fixture writes the three new files.
- [x] 3.5 Add a `render_design_point_power` figure (orientation `angle` power vs `n_samples`, one line per ρ, with MC error bars, eigengap median annotated) written beside the CSV when design records exist; verify the report test asserts the PNG is produced and the no-design-grid path writes none.

## 4. Pilot profile and documentation

- [x] 4.1 Commit `examples/trajectory_power_study/phase5_design_point_pilot.json` per design D7 (p_dmp 0.1, no `surgery_censoring` key, design grid ρ × n, four modes, effects 0/0.25/0.5/1.0, 100 reps, 199 perms, attribution off, gate off, `type_i` target kept, `design_point` rule) and verify enumeration succeeds with no headroom rejection and the expected cell count via a `tests/test_study_phase4.py`-style profile test that loads the committed file and calls `enumerate_study`.
- [x] 4.2 Add a Phase 5 design-point pilot section and a `design_grid` row to `examples/trajectory_power_study/README.md`, and a `design_grid` sentence to the `study/` bullet in `CLAUDE.md`; verify by reading the rendered README and that `uv run ruff check src/ tests/` and `uv run mypy src/motco/` pass.
- [x] 4.3 Run the pre-commit gate (`uv run ruff check src/ tests/ && uv run mypy src/motco/ && MOTCO_TEST_PERMS=99 uv run pytest tests/ -m "not slow" --tb=short`) and verify all pass.

## 5. Run the pilot and record the decision

- [ ] 5.1 Run the pilot on SLURM (or locally in shards) with `scripts/run_study_shard.py` over `phase5_design_point_pilot.json`, without `--n-jobs`, into `results/phase5-design-point-<date>/`; verify with `motco_study.py merge` that every enumerated (cell, replicate) unit is present once with a matching parameter signature and zero failures (or failures recorded and rerun).
- [ ] 5.2 Generate the report (`motco_study.py report`) and verify `design_point_operating.csv`, `continuity_resolved_orientation.csv`, `design_point_decision.json`, and the figure exist and that the baseline column's power curves match the Phase 4 profile's shape at n = 300 within Monte Carlo error.
- [ ] 5.3 Write `docs/reports/phase5-design-point-pilot-<date>.md` (config, code revision, versions, unit/failure counts, decision verdict, per-column orientation power beside eigengap and null-width dispersion, per-column anchor Type I rates, continuity-conditional interpretation, limitations, reproduction commands) and verify every number cites a CSV/JSON path.
- [ ] 5.4 Update `docs/phase5-readiness.md` item 4 with the chosen design point or revised claim and the hand-off to item 3, and update `docs/roadmap.md`'s Phase 4→5 paragraph; verify the header status line lists item 4 as resolved (or revised) with the report link.
